type Query {
  topCoins(vs_currency: String!): [Coin]!
  coinChartData(
    id: String!
    vs_currency: String!
    days: String!
    interval: String!
  ): [CoinChartData]!
  userCredentials(API_key: String!, secret: String!): Boolean!
}
