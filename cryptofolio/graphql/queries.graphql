type Query {
  # coingeko
  topCoins(vs_currency: String!): [Coin]!
  coinChartData(
    id: String!
    vs_currency: String!
    days: String!
    interval: String!
  ): [CoinChartData]!

  # binance
  binanceAccountData(
    API_key: String!
    secret: String!
    recvWindow: Int
  ): binanceAccount
}
