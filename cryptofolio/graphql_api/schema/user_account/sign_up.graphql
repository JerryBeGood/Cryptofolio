extend type Mutation {
  signUp(email: String!, password: String!): AuthPayload!
  activateAccount(
    email: String!
    password: String!
    code: String!
  ): AuthPayload!
}

type AuthPayload {
  Success: Boolean!
  Token: String
}
