extend type Mutations {
  singUp(email: String!, password: String!): AuthPayload!
  activate(email: String!, password: String!, code: String!): AuthPayload!
}

type AuthPayload {
  Success: Boolean!
  Token: String
}
